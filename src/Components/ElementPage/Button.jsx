import { StyledButton } from '../styles/ElementPages.styled/Button.styled';

const Button = (props) => {
  return (
    <StyledButton>
      <h3>{props.title}</h3>
    </StyledButton>
  );
  {
  }
};

export { Button };
